services:
  server:
    build:
      context: .
    env_file: .env
    ports:
      - 3000:3000

  postgres:
    image: postgres:latest
    container_name: my_postgres
    restart: always
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
